
/**
 * @param values number[] // 各个节点的价值
 * @param edges [idxA, idxB, number][] 每两个节点之间的路径耗时
 * @param maxTime 所有价值路径的最大耗时限制
 * 
 * 合法路径: 路径可以重复走, 但是一个节点最后计入一个价值
 * 一个节点，最多四条边
 */
function maximalPathQuality(values: number[], edges: number[][], maxTime: number): number {
  // while ()
};
