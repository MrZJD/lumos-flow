import React, { useEffect } from 'react';
import * as d3 from 'd3';


export default () => {
  useEffect(() => {
    // const drag = d3.drag();

    // d3.select('').call(d3.drag).on('drag');
  }, []);

  return (
    <div>
      <h4>Dragging API</h4>
    </div>
  );
};
